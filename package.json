{
  "name": "studymate",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run python-server\"",
    "backend": "cd backend && cd src && node index.js",
    "frontend": "cd frontend && npm start",
    "python-server": "cd backend && cd src && python extract_text.py"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "@fortawesome/free-solid-svg-icons": "^6.6.0",
    "@fortawesome/react-fontawesome": "^0.2.2"
  }
}
